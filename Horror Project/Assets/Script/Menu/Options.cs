using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NewBehaviourScript : MonoBehaviour
{
    /*
    /// RESOLUCION
    Resolution[] resolutions = Screen.resolutions;
    Screen.SetResolution(Ancho, alto, pantallaCompleta);

    /// CALIDAD VISUAL GENERAL
    QualitySettings.SetQualityLevel(level);

    /// VOLUMENES SONIDO
    mixer.SetFloat("BGMVOL", volumen1);
    mixer.SetFloat("SFXVOL", volumen2);

    // Pasar de "db" a 1 % para la barra de ajustes de juego...
    float decibelios = 20 * Mathf.Log10(porcentaje / 100);

    /// CAMBIAR CONTROLES

    */

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
